import { jsx as _jsx } from "react/jsx-runtime";
import { listToClass } from '@stack-spot/portal-theme';
import { isNil } from 'lodash';
import { applyCSSVariable } from '../utils/css.js';
import { withRef } from '../utils/react.js';
import { CitricComponent } from './CitricComponent.js';
/**
 * Shows a circular progress. This can indicate a loading state (spinner) or the progress of a task. When "progress" is not set, the
 * spinner is animated, indicating a busy state.
 *
 * @example
 * A simple loading with a Progress Circular.
 * ```
 * <ProgressCircular />
 * ```
 * A Progress Circular that is 50% complete.
 * ```
 * <ProgressCircular progress={50} />
 * ```
 */
export const ProgressCircular = withRef(({ progress, size, style, className, ...props }) => _jsx(CitricComponent, { tag: "div", component: "progress-circular", className: listToClass([size, className, isNil(progress) && 'indeterminate']), style: progress === undefined ? style : applyCSSVariable(style, 'progress', progress), ...props }));
//# sourceMappingURL=ProgressCircular.js.map