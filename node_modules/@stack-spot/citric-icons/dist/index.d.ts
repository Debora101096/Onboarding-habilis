import type { CitricIconFill as Fill } from './types/fill';
import type { CitricIconOutline as Outline } from './types/outline';
import type { CitricIconSocial as Social } from './types/social';
export type { CitricIconFill } from './types/fill';
export type { CitricIconOutline } from './types/outline';
export type { CitricIconSocial } from './types/social';
export type IconGroup = 'fill' | 'outline' | 'social';
interface WithBaseIcon {
    /**
     * The icon group.
     *
     * @default 'outline'
     */
    group?: IconGroup;
    /**
     * The icon name.
     */
    icon: Fill | Social | Outline;
}
interface WithOutlineIcon extends WithBaseIcon {
    group?: 'outline';
    icon: Outline;
}
interface WithFillIcon extends WithBaseIcon {
    group: 'fill';
    icon: Fill;
}
interface WithSocialIcon extends WithBaseIcon {
    group: 'social';
    icon: Social;
}
export type WithIcon = WithOutlineIcon | WithFillIcon | WithSocialIcon;
export interface BaseIconProps<T extends IconGroup> {
    /**
     * The icon group.
     *
     * @default 'outline'
     */
    group?: T;
    /**
     * The icon name.
     */
    icon: T extends 'fill' ? Fill : (T extends 'social' ? Social : Outline);
    /**
     * The size of the icon.
     *
     * - xxl: 32px;
     * - xl: 22px;
     * - lg: 18px;
     * - md: 14px;
     * - sm: 12px;
     * - xs: 10px;
     * - xxs: 8px.
     *
     * @default "the font-size in the current context"
     */
    size?: 'xxs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | 'xxl';
}
export type IconProps<T extends IconGroup> = React.JSX.IntrinsicElements['i'] & BaseIconProps<T>;
/**
 * Renders an icon based on the properties "icon" and "group".
 *
 * There are three icon groups:
 * - outline: most of the icons are in this group. The icons are stroke only.
 * - fill: most outline icons have a fill version. Fill icons are filled with solid colors, they're not just strokes.
 * - social: icons for brands (logos).
 *
 * By default, the group is "outline".
 *
 * The icon size depends on the font-size of the current context. To set a specific size, use the property "size".
 *
 * To set the icon color, use the css property "color".
 *
 * @example
 *
 * ```
 * <Icon icon="Search" />
 * ```
 */
export declare function Icon<T extends IconGroup = 'outline'>({ className, group, icon, size, ...props }: IconProps<T>): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=index.d.ts.map