/// <reference types="react" />
import type * as datadogT from '@datadog/browser-rum';
import { EventPageDetails, Portal } from './event-bus-events.js';
interface DatadogInitProps extends datadogT.RumInitConfiguration {
    tenant?: 'stackspot' | 'itau';
}
export declare function isDatadogInitialized(): boolean;
/**
 * Initializes Datadog RUM, allowing tracing only for URLs from the selected tenant's API catalog.
 *
 * @param config Datadog RUM configuration, including the tenant to select the allowed API catalog.
 */
export declare function datadogInit({ applicationId, clientToken, env, service, tenant, ...rest }: DatadogInitProps): Promise<void>;
export declare function useDatadogSetUser(): void;
/**
 * Creates an event in the analytics tool saying an action was performed.
 * @param action the action name.
 * @param context a free-form object where you can pass any relevant information.
 */
export declare function trackAction(action: string, context?: object | undefined): Promise<void>;
/**
 * Creates an event in the analytics tool saying an action was performed. All the data is extracted from the JS Event.
 * @param event the js event.
 * @param context a free-form object where you can pass any relevant information.
 */
export declare function trackEvent(event: React.UIEvent, context?: Record<string, any>): void;
/**
 * Sends an error to the remote tracking tool.
 * @param error the error object.
 * @param context a free-form object where you can pass any relevant information.
 */
export declare function trackError(error: unknown, context?: object | undefined): Promise<void>;
export declare function sendClickToEventBus(itemText: string, routeDetails: EventPageDetails, portal: Portal, sendToEventBus?: boolean): Promise<void>;
export declare function trackPageLoaded(routeDetails: EventPageDetails, portal: Portal, customerId?: string, sendToEventBus?: boolean): Promise<void>;
export declare function formatRouteForAnalytics(path: string, params: Record<string, any>): {
    pathname: string;
    pathNameWithParams: string;
    searchParams: string;
    searchParamsWithVariableName: string;
    routeParams: Record<string, any>;
};
/**
 * @deprecated
 * Use the hook `usePageTracking` instead.
 */
export declare function getRouteForAnalytics({ route, params }: {
    route: Record<string, any>;
    params: Record<string, any>;
}): {
    pathname: string;
    pathNameWithParams: string;
    searchParams: string;
    searchParamsWithVariableName: string;
    routeParams: Record<string, any>;
};
/**
 * Whenever "path" or "params" change, a new analytics entry for a page navigation is created.
 * @param path the path to the current page, may include path variables as "{variable_name}".
 * @param params the parameters in the path: route params and query params.
 */
export declare function usePageTracking(path: string, params: Record<string, any>, portal: Portal): void;
export {};
//# sourceMappingURL=index.d.ts.map