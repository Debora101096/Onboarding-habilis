/**
 * OpenAPI definition
 * v0
 * DO NOT MODIFY - This file has been generated using oazapfts.
 * See https://www.npmjs.com/package/oazapfts
 */
import * as Oazapfts from "@oazapfts/runtime";
import * as QS from "@oazapfts/runtime/query";
export const defaults = {
    headers: {},
    baseUrl: "http://api-management-catalog-assets-api.stg.stackspot.com",
};
const oazapfts = Oazapfts.runtime(defaults);
export const servers = {
    generatedServerUrl: "http://api-management-catalog-assets-api.stg.stackspot.com"
};
export function list({ size, page, tag, search }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/tags${QS.query(QS.explode({
        size,
        page,
        tag,
        search
    }))}`, {
        ...opts
    }));
}
export function createTag({ createTagRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson("/v1/tags", oazapfts.json({
        ...opts,
        method: "POST",
        body: createTagRequest
    })));
}
export function getObjectSchemas(opts) {
    return oazapfts.ok(oazapfts.fetchText("/v1/schemas", {
        ...opts
    }));
}
export function createSchema({ createSchemaRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson("/v1/schemas", oazapfts.json({
        ...opts,
        method: "POST",
        body: createSchemaRequest
    })));
}
export function uploadSchemaArtifact({ schemaId, versionId, contentLength }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(versionId)}/artifacts`, {
        ...opts,
        method: "POST",
        headers: oazapfts.mergeHeaders(opts?.headers, {
            "Content-Length": contentLength
        })
    }));
}
export function getEntries({ schemaId, schemaVersionId, queryParams, allRequestParams }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries${QS.query(QS.explode({
        queryParams,
        allRequestParams
    }))}`, {
        ...opts
    }));
}
export function createEntry({ schemaId, schemaVersionId, createEntryRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries`, oazapfts.json({
        ...opts,
        method: "POST",
        body: createEntryRequest
    })));
}
export function getSubSchemas(opts) {
    return oazapfts.ok(oazapfts.fetchText(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas`, {
        ...opts
    }));
}
export function createSubSchema({ schemaId, createSubSchemaRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas`, oazapfts.json({
        ...opts,
        method: "POST",
        body: createSubSchemaRequest
    })));
}
export function uploadSchemaArtifact1({ schemaId, subSchemaId, versionId, contentLength }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(versionId)}/artifacts`, {
        ...opts,
        method: "POST",
        headers: oazapfts.mergeHeaders(opts?.headers, {
            "Content-Length": contentLength
        })
    }));
}
export function getSubEntries({ schemaId, subSchemaId, subVersionId, allRequestParams, queryParams }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries${QS.query(QS.explode({
        allRequestParams,
        queryParams
    }))}`, {
        ...opts
    }));
}
export function createSubEntry({ schemaId, subSchemaId, subVersionId, createSubEntryRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries`, oazapfts.json({
        ...opts,
        method: "POST",
        body: createSubEntryRequest
    })));
}
export function getObjectSchemaId({ schemaId }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}`, {
        ...opts
    }));
}
export function createSchemaVersion({ schemaId, jsonNode }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: jsonNode
    })));
}
export function getEntryById({ schemaId, schemaVersionId, entryId, presenter }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}${QS.query(QS.explode({
        presenter
    }))}`, {
        ...opts
    }));
}
export function updateEntry({ schemaId, schemaVersionId, entryId, updateEntryRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: updateEntryRequest
    })));
}
export function updateTag({ schemaId, schemaVersionId, entryId, oldTag, updateEntryTag }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}/tags/${encodeURIComponent(oldTag)}`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: updateEntryTag
    })));
}
export function downloadObjectArtifacts({ schemaId, schemaVersionId, entryId, name }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}/artifacts${QS.query(QS.explode({
        name
    }))}`, {
        ...opts
    }));
}
export function attachArtifacts({ schemaId, schemaVersionId, entryId, jsonNode }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}/artifacts`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: jsonNode
    })));
}
export function downloadSchemaArtifact({ schemaId, schemaVersionId, entryId, artifactId }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}/artifacts/${encodeURIComponent(artifactId)}`, {
        ...opts
    }));
}
export function updatedArtifact({ artifactId, schemaId, schemaVersionId, entryId, contentLength }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}/artifacts/${encodeURIComponent(artifactId)}`, {
        ...opts,
        method: "PATCH",
        headers: oazapfts.mergeHeaders(opts?.headers, {
            "Content-Length": contentLength
        })
    }));
}
export function getSubSchema({ schemaId, subSchemaId }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}`, {
        ...opts
    }));
}
export function newVersion({ schemaId, subSchemaId, jsonNode }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: jsonNode
    })));
}
export function updateTag1({ schemaId, subSchemaId, subVersionId, subEntryId, oldTag, updateEntryTag }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}/tags/${encodeURIComponent(oldTag)}`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: updateEntryTag
    })));
}
export function downloadObjectArtifacts1({ schemaId, subSchemaId, subVersionId, subEntryId, name }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}/artifacts${QS.query(QS.explode({
        name
    }))}`, {
        ...opts
    }));
}
export function attachArtifacts1({ schemaId, subSchemaId, subVersionId, subEntryId, jsonNode }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}/artifacts`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: jsonNode
    })));
}
export function downloadSchemaArtifact1({ schemaId, subSchemaId, subVersionId, subEntryId, artifactId }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}/artifacts/${encodeURIComponent(artifactId)}`, {
        ...opts
    }));
}
export function updatedArtifact1({ artifactId, schemaId, subSchemaId, subVersionId, subEntryId, contentLength }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}/artifacts/${encodeURIComponent(artifactId)}`, {
        ...opts,
        method: "PATCH",
        headers: oazapfts.mergeHeaders(opts?.headers, {
            "Content-Length": contentLength
        })
    }));
}
export function updateEntry1({ schemaId, subSchemaId, subVersionId, entryId, updateEntryRequest }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(entryId)}`, oazapfts.json({
        ...opts,
        method: "PATCH",
        body: updateEntryRequest
    })));
}
export function getTag({ tag }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/tags/${encodeURIComponent(tag)}`, {
        ...opts
    }));
}
export function getSchemaVersions(opts) {
    return oazapfts.ok(oazapfts.fetchText(`/v1/schemas/${encodeURIComponent(schemaId)}/versions`, {
        ...opts
    }));
}
export function getSchemaVersionById({ schemaId, versionId }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(versionId)}`, {
        ...opts
    }));
}
export function getSchemaVersions1(opts) {
    return oazapfts.ok(oazapfts.fetchText(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions`, {
        ...opts
    }));
}
export function getSchemaVersionById1(opts) {
    return oazapfts.ok(oazapfts.fetchText(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(versionId)}`, {
        ...opts
    }));
}
export function getSubEntriesById({ schemaId, subSchemaId, subVersionId, subEntryId }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}`, {
        ...opts
    }));
}
export function removeTag({ schemaId, schemaVersionId, entryId, tag }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/versions/${encodeURIComponent(schemaVersionId)}/entries/${encodeURIComponent(entryId)}/tags/${encodeURIComponent(tag)}`, {
        ...opts,
        method: "DELETE"
    }));
}
export function removeTag1({ schemaId, subSchemaId, subVersionId, subEntryId, tag }, opts) {
    return oazapfts.ok(oazapfts.fetchJson(`/v1/schemas/${encodeURIComponent(schemaId)}/subschemas/${encodeURIComponent(subSchemaId)}/versions/${encodeURIComponent(subVersionId)}/entries/${encodeURIComponent(subEntryId)}/tags/${encodeURIComponent(tag)}`, {
        ...opts,
        method: "DELETE"
    }));
}
//# sourceMappingURL=apiManagement.js.map