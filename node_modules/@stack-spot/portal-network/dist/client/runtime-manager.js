import { defaults, getAppInfo, getAppsInRealm, getPluginDeployments, getRunByUlid, getRunsOnAllEnvs, getRunsOnEnv, getRunsRunning, getRunTaskPlugins, getRunTasks, runRollbackApp1, runRollbackInfra1 } from '../api/apiRuntime.js';
import { DefaultAPIError } from '../error/DefaultAPIError.js';
import { rteDictionary } from '../error/dictionary/rte.js';
import { ReactQueryNetworkClient } from '../network/ReactQueryNetworkClient.js';
import { getApiAddresses } from '../api-addresses.js';
class RuntimeManagerClient extends ReactQueryNetworkClient {
    constructor() {
        super(getApiAddresses().apiRuntime.url, defaults);
        /**
         * Runs an app rollback
         */
        Object.defineProperty(this, "appRollback", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.mutation(runRollbackApp1)
        });
        /**
         * Runs an infra rollback
         */
        Object.defineProperty(this, "infraRollback", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.mutation(runRollbackInfra1)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting all runnings run
         */
        Object.defineProperty(this, "adminGetRunsRunning", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getRunsRunning)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting all runs off an app or infra on an env
         */
        Object.defineProperty(this, "adminGetRunsOnEnv", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getRunsOnEnv)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting all runs off an app or infra on every environment its deployed
         */
        Object.defineProperty(this, "adminGetRunsOnAllEnvs", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getRunsOnAllEnvs)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting run data by ulid identifier
         */
        Object.defineProperty(this, "adminGetRunByUlid", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getRunByUlid)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting run task data of a run
         */
        Object.defineProperty(this, "adminGetRunTasks", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getRunTasks)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting run task plugin data of a run
         */
        Object.defineProperty(this, "adminGetRunTaskPlugins", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getRunTaskPlugins)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting plugin deployment data of a run
         */
        Object.defineProperty(this, "adminGetPluginDeployments", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getPluginDeployments)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting the list of apps in a realm
         */
        Object.defineProperty(this, "adminGetAppsInRealm", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getAppsInRealm)
        });
        /**
         * Internal use only (Only Authorized tokens) - Admin endpoint for getting app or infra information by ulid
         */
        Object.defineProperty(this, "adminGetAppInfo", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: this.query(getAppInfo)
        });
    }
    buildStackSpotError(error) {
        return new DefaultAPIError(error.data, error.status, rteDictionary, error.headers);
    }
}
export const runtimeManagerClient = new RuntimeManagerClient();
//# sourceMappingURL=runtime-manager.js.map